/**
 * @deprecated Legacy shim - prefer @/features/tables/hooks
 * 
 * This hook delegates to the feature-owned useSession.
 * Use useSession directly from @/features/tables/hooks for new code.
 */

export { useSession } from '@/features/tables/hooks';

